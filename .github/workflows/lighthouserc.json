{
    "ci": {
      "assert": {
        "assertions": {
          "first-contentful-paint": ["error", { "minScore": 0.6 }]
        }
      },
      "collect": {
        "startServerCommand": "curl https://get.volta.sh | bash | source ~/.bashrc && volta install node && npm i && npm run build && npm run start"
      }
    }
  }