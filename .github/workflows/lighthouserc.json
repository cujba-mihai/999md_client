{
    "ci": {
      "assert": {
        "assertions": {
          "first-contentful-paint": ["error", { "minScore": 0.6 }]
        }
      },
      "collect": {
        "startServerCommand": "curl https://get.volta.sh | bash && volta install node && npm i && npm build && npm run start"
      }
    }
  }