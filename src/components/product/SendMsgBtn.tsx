'use client';
import React from "react";
import Button from '../button/Button';

const SendMsgBtn = () => {
    return (<Button btnText='Trimite mesaj' onClick={() => {}} type="primary" />);
}

export default SendMsgBtn;